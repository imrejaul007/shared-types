{
  "name": "rez-api-gateway-worker",
  "version": "1.0.0",
  "description": "REZ API Gateway — Cloudflare Worker WAF",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy --env production",
    "dev": "wrangler dev --env production",
    "deploy:dry": "wrangler deploy --dry-run --env production",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0",
    "wrangler": "^3.0.0"
  },
  "dependencies": {
    "itty-router": "^4.2.0"
  }
}
